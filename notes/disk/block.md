#### Block
- A block is the smallest unit of data storage on a disk.
- Disks are divided into fixed-size blocks (e.g., 4 KB).
- When you read or write data, it happens in block units, not byte-by-byte.
- Even a small file may occupy a full block.